.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/bootstrap/ssr
/node_modules
# public/build IS committed on purpose: the shared host that serves this app
# has Composer but no Node, so `git pull` has to deliver ready-made assets.
# Run `npm run build` before every deploy commit.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
auth.json
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log

# Payment provider key material - never committed.
/storage/app/dana
/storage/app/doku
*.pem
*.key
